// // calculating satellite data from TLE
// // assume TLE.split(" ")

// var eA, tA; // eccentric and true anomolies
// var mA = // 2nd line, index[6], NNN.NNNN format
// var ec = // 2nd line, index[4], need to add a decimal point before it
// var dp = 10 // decimal places requested


// eA = EccAnom(ec, mA, dp);
// tA = TrueAnom(ec, eA, dp); // the angle from perigee. RA + perigee + tA = longitude relative to Aries
